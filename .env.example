# Environment Variables
#
# Copy this file to .env.local and fill in the values.
# Never commit .env.local to git.

# --- Auth0 Configuration ---
# Your Auth0 tenant domain (e.g. "your-tenant.us.auth0.com")
AUTH0_DOMAIN=

# The Auth0 API identifier / audience (e.g. "https://mapp-intelligence-mcp.vercel.app/api/mcp")
AUTH0_AUDIENCE=

# Shared secret for Auth0 Action redirect session tokens (HS256).
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH0_ACTION_SECRET=

# --- Settings Page Auth0 Application ---
# A Regular Web Application in Auth0 used for the /settings page login flow.
AUTH0_SETTINGS_CLIENT_ID=
AUTH0_SETTINGS_CLIENT_SECRET=

# These are exposed to the browser (NEXT_PUBLIC_ prefix):
NEXT_PUBLIC_AUTH0_DOMAIN=
NEXT_PUBLIC_AUTH0_AUDIENCE=
NEXT_PUBLIC_AUTH0_SETTINGS_CLIENT_ID=

# --- Encryption ---
# 64-character hex string (32 bytes) for AES-256-GCM encryption of stored credentials.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
CREDENTIAL_ENCRYPTION_KEY=

# --- Mapp API Endpoint ---
# Only supported endpoint currently:
MAPP_API_BASE_URL=https://intelligence.eu.mapp.com

# --- Upstash Redis (Vercel KV replacement) ---
# These are auto-provisioned when you add an Upstash Redis store to your Vercel project.
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# --- Optional ---
# NODE_ENV=production
